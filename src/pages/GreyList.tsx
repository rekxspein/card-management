import { Box } from '@mui/material';
import { FC } from 'react';

export const GreyListPage: FC = () => {
  return (
    <Box
      sx={{
        height: '100%',
        width: '100%',
        display: 'flex',
        flexDirection: 'column'
      }}
    >
      Grey List Demo
    </Box>
  );
};
