import { Box } from '@mui/material';
import { FC } from 'react';
const Home: FC = () => {
  return (
    <Box
      sx={{
        height: '100%',
        width: '100%',
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        justifyContent: 'center'
      }}
    >
      DashBoard!!!
    </Box>
  );
};

export default Home;
